#:schema https://json.schemastore.org/any.json

env_files = []
root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
args_bin = []
bin = "./tmp/main"
cmd = "go build -o ./tmp/main ."
delay = 1000
entrypoint = ["./tmp/main"]
exclude_dir = ["assets", "tmp", "vendor", "testdata", "static"]
exclude_file = []
exclude_regex = ["_test.go"]
exclude_unchanged = false
follow_symlink = false
full_bin = ""
ignore_dangerous_root_dir = false
include_dir = ["views", "styles"]
# Added "templ" and "css" so Air watches these file extensions
include_ext = ["go", "tpl", "tmpl", "html", "templ", "css"]
include_file = []
kill_delay = "0s"
log = "build-errors.log"
poll = false
poll_interval = 0
post_cmd = []
# General pre_cmd for non-Windows (Mac/Linux)
pre_cmd = [
  "templ generate",
  "./tailwindcss -i ./styles/tailwind.css -o ./static/css/styles.css",
]
rerun = false
rerun_delay = 500
send_interrupt = false
stop_on_error = true

[build.windows]
args_bin = []
bin = "tmp\\main.exe"
cmd = "go build -o ./tmp/main.exe ."
entrypoint = ["tmp\\main.exe"]
full_bin = ""
post_cmd = []
# Windows-specific pre_cmd using your local tailwindcss.exe
pre_cmd = [
  "templ generate",
  ".\\tailwindcss.exe -i .\\styles\\tailwind.css -o .\\static\\css\\styles.css",
]

[color]
app = ""
build = "yellow"
main = "magenta"
mode = ""
runner = "green"
watcher = "cyan"

[log]
main_only = false
silent = false
time = false

[misc]
clean_on_exit = false

[proxy]
app_port = 8080
# Set timeout to 5000 milliseconds (5 seconds) so the proxy waits for main.exe to spin up
app_start_timeout = 5000
enabled = true
proxy_port = 8090

[screen]
clear_on_rebuild = false
keep_scroll = true
